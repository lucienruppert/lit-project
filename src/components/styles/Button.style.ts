import { css } from "lit";

export const buttonStyle = css`
  button {
    cursor: pointer;
    background-color: var(--highlight);
    color: var(--white);
    border: none;
    border-radius: 3px;
    padding: 10px 10px;
  }
`;
